{
  "version": 1,
  "note": "Plain-language work intent for PMO-dispatched issues. generate_job_memos.py reads this — update when triage dispatches.",
  "tasks": {
    "ISSUE-BUS-001": {
      "landed_audit": "off",
      "problem": "When the agent-bus starts a coding worker, it calls `worker_model.py` to pick an OpenRouter model slug. `bus.py` was passing an empty string as the registry model when none is configured; that breaks resolution and can crash worker spawn — jobs then stall as held/queued behind a dead harness.",
      "doing": "Reproduce the crash, patch model resolution in `agent-bus/scripts/bus.py` and/or `worker_model.py`, add a unit test, and prove the witness exits 0 so workers can spawn again.",
      "steps": [
        "Reproduce — exercise `resolve_or_ccr_default()` with coding_worker / PMO packets (empty registry model)",
        "Patch — pass `None` instead of `\"\"` when no model in session meta (bus.py ~line 992)",
        "Test — add `test_worker_model.py` under agent-bus/scripts and run it",
        "Witness — job completes only when tests pass and a worker can spawn without model error"
      ],
      "witness": "python3 agent-bus/scripts/test_worker_model.py (or test_dispatch_policy.py) exits 0",
      "touch_paths": [
        "agent-bus/scripts/worker_model.py",
        "agent-bus/scripts/bus.py",
        "agent-bus/scripts/test_worker_model.py"
      ]
    },
    "ISSUE-80": {
      "shipped_on_main": true,
      "landed_audit": "open_witness",
      "problem": "The Nick2 dashboard can lag reality: ledger and bus state are exported on a schedule, and job memos were too thin for Nick to see what workers are actually doing.",
      "doing": "**Done — no bus dispatch.** POL-003 on main (4d73ceb): bus_finish_sync, sync-dashboard-live cron, export_bus_live + generate_job_memos, witness_dashboard_honesty. SYS-002/JOB-924 completed earlier same day.",
      "steps": [
        "Wire reconcile-ledger + export_bus_live into sync-dashboard-live.sh cron",
        "Ensure generate_job_memos runs on every bus-live export",
        "Add/strengthen witness_dashboard_honesty.py checks",
        "Prove 15m cron + manual sync keep dashboard honest"
      ],
      "witness": "python3 scripts/witness_dashboard_honesty.py exits 0 on droplet",
      "touch_paths": [
        "scripts/sync-dashboard-live.sh",
        "scripts/export_bus_live.py",
        "scripts/generate_job_memos.py",
        "scripts/reconcile-ledger.py"
      ]
    },
    "ISSUE-ROUTING-001": {
      "shipped_on_main": true,
      "landed_audit": "open_witness",
      "problem": "PMO queued re-implementation of routing that already shipped on main.",
      "doing": "**Done — no bus dispatch.** `worker_model.py` resolves tier + escalate on retry; `bus.py` calls it at worker spawn; policy in `references/model-routing.yaml`.",
      "steps": [
        "9186153 — task-aware OpenRouter routing via worker_model",
        "ff874d4 — registry_model kwarg fix on resolve_or_ccr_default",
        "Optional follow-up: dedicated test_worker_model.py (not blocking)"
      ],
      "witness": "python3 agent-bus/scripts/test_dispatch_policy.py exits 0; workers log model: slug (tier=...) at spawn",
      "touch_paths": [
        "agent-bus/scripts/worker_model.py",
        "agent-bus/scripts/bus.py",
        "references/model-routing.yaml"
      ]
    },
    "ISSUE-15": {
      "landed_audit": "off",
      "problem": "~107 far-field jesus-pattern skills may pollute router/digest; issue is p3 and decision-gated (Option A: tag tiers + prefer Tier 1–2 vs Option B: leave indexed).",
      "doing": "**Nick's queue — not running on agents.** Pick Option A or B, define tier schema in frontmatter, then re-open with a runnable witness.",
      "steps": [
        "Decide Option A (usefulness_tier in frontmatter + router prefers Tier 1–2) vs Option B (status quo)",
        "If A: document schema in GitHub #15 and job catalog witness",
        "Dispatch only after witness is concrete (e.g. skill_manifest selftest + router smoke)"
      ],
      "witness": "Decision recorded on #15; no bus dispatch until Nick opts in",
      "touch_paths": [
        "Projects-for-agents/skill-radar/skill_manifest.py",
        "skill-router/skill_index.py",
        "Projects-for-agents/skill-radar/run_daily.sh"
      ]
    },
    "ISSUE-24": {
      "landed_audit": "off",
      "problem": "Telegram PA bot currently runs with bypassPermissions (full root for Nick's Telegram ID only). Nick may want read-only or acceptEdits instead — this is a personal decision, not an agent task.",
      "doing": "**Nick's queue — not running on agents.** When Nick picks it up: compare bypassPermissions vs BRIDGE_PERMISSION_MODE=default vs acceptEdits; decide; optionally systemctl restart telegram-bridge.",
      "steps": [
        "Review telegram-bridge/control.py and current auth",
        "Draft options with risk tradeoffs",
        "Ledger decision memo — gate only if new risk"
      ],
      "witness": "Decision memo in ledger or memos/queue",
      "touch_paths": ["telegram-bridge/"]
    }
  }
}