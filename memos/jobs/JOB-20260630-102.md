# JOB-102 — Replace WIP execution brief template with McKinsey-style memo (POL-004).

**Full ID:** `JOB-20260630-102` · [← Dashboard](https://nicholasg3.github.io/nick2-dashboard/index.html)

## SITUATION

Agent-bus coding/research worker packet.

## WHAT IT'S DOING

Replace WIP execution brief template with McKinsey-style memo (POL-004).

**Steps:**
1. Witness per objective

**Done when:** witness in objective passes (see OBJECTIVE below)

## WHERE IT STANDS

Bus status **completed** (worker `done`).

**Latest worker report:**

{
  "status": "completed",
  "bottom_line": "Replaced WIP execution brief template with McKinsey-style memo format (POL-004) and updated all active mission briefs.",
  "changes_made": [
    "Updated scripts/execution_brief.py with new schema (situation, mece[], paths_considered[], chosen_path_why, where_it_stands, effort{time,work,budget})",
    "Converted PMO-001, P-001, and LIT-001 to McKinsey format with real content (no placeholders)",
    "Added validate_brief() function that refuses generation if any core section is empty/placeholder",
    "Enhanced ceo_focus_line() to extract meaningful…

## EFFORT & COST

- **Time:** Time in state: **8h 41m ago** · last touch **8h 5m ago**
- **Work:** Work: `dashboard_worker` on `nick2-dashboard` · branch `job/20260630-102-replace-wip-execution-brief-te`
- **Budget:** Budget: spent $0.00 · remaining $20.00 · limit $20.00/week

## LINKS

- CEO ledger: `logs/ceo-ledger.jsonl`
- Bus packet: `agent-bus/logs/JOB-20260630-102.packet.json`

## OBJECTIVE (full)

```
Replace WIP execution brief template with McKinsey-style memo (POL-004).

USER TEMPLATE — every section real content, no placeholders:

[← Dashboard]   <date>
<ID>: <plain-language title — spoken outcome, ~8 words, no API/file/cron/gate IDs in title>

SITUATION — 1-2 sentences why on board
MECE DECOMPOSITION — real mutually-exclusive branches, each one-line current state; must partition problem not restate title
PATHS CONSIDERED — options genuinely on table, one line each
CHOSEN PATH + WHY — headline section, substantive prose explaining tradeoff vs other paths (never single line)
WHERE IT STANDS — plain prose done/in-flight; blockers ONLY inline here if explaining current state
EFFORT & COST — always all three: Time (time-in-state + blocked portion), Work (dispatches/retries), Budget (spent/remaining/limit)
LINKS — working links only (Dashboard, Ledger)

CUT: ASCII progress bars, %, Critical Path boilerplate, standalone Blockers/Waiting-On/Milestones/Recent Events sections, demoted gated/blocked callout as headline (POL-002 stale note = small footer only).

DATA: Refactor EXECUTION_BRIEFS in scripts/execution_brief.py to new schema (situation, mece[], paths[], chosen_path_why, where_it_stands, effort{time,work,budget}). Populate REAL content for PMO-001, SYS-002, P-001, LIT-001 (idle uses same template). Add validate_brief() — refuse generate-memos if any core section empty/placeholder (TBD, —, _None_).

CEO FOCUS (dashboard/app.js + generate-memos current header): one plain sentence e.g. "Dashboard live-sync — executing chosen path (extend gate_chat_server), ~30% through" — never truncated title, never clip mid-word. Title format on memo: PROJECT-ID: spoken outcome.

Document in memos/policy.md as POL-004. Regenerate all queue/current memos. Push main.

WITNESS: python3 scripts/generate-memos.py && python3 -c "from execution_brief import validate_brief, EXECUTION_BRIEFS; [validate_brief(k,v) for k,v in EXECUTION_BRIEFS.items()]" exits 0.
```

## CONSTRAINTS

- Keep repo private.
- Do not mutate secrets.
- Work only on your job branch; do not merge to main.
- Report back with summary, files changed, tests run, blockers, and branch name.

