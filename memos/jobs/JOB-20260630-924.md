# JOB-924 — fix memo/ledger/bus lag + POL-002 server enforcement.

**Full ID:** `JOB-20260630-924` · [← Dashboard](https://nicholasg3.github.io/nick2-dashboard/index.html)

## SITUATION

Ledger mission **SYS-002** was dispatched to the agent-bus. Part of **DISPATCH-001** batch (pmo-dispatch:queued 3 issues within $3.0 planned spend. Queue: ISSUE-BUS-001(#None), ISSUE-80(#80), ISSUE-15(#15)). Portfolio task: *Make the dashboard live*. Dashboard live-sync (no React): fix memo/ledger/bus lag + POL-002 server enforcement.

## WHERE IT STANDS

Bus status **completed** (worker `done`).

**Latest worker report:**

{
  "status": "completed",
  "bottom_line": "Let me document the POL-002 implementation in policy.md first, then implement the other parts:",
  "changes_made": [],
  "evidence": [],
  "questions_for_nicholas": [],
  "branch": "job/20260630-924-dashboard-live-sync-no-react-f",
  "diff_stat": "(no diff vs main)",
  "job_id": "JOB-20260630-924",
  "session": "dashboard_worker",
  "display_name": "dashboard-live-sync-no-react-fix-memo-le [dashboard] \u00b7 reporting",
  "finished_at": "2026-06-30T12:14:39Z"
}

## EFFORT & COST

- **Time:** Time in state: **3h 19m ago** · last touch **2h 33m ago**
- **Work:** Work: `dashboard_worker` on `nick2-dashboard` · branch `job/20260630-924-dashboard-live-sync-no-react-f`
- **Budget:** Budget: spent $0.00 · remaining $20.00 · limit $20.00/week

## LINKS

- Portfolio brief: [SYS-002](https://nicholasg3.github.io/nick2-dashboard/memos/queue/SYS-002.html)
- CEO ledger: `logs/ceo-ledger.jsonl` (search `SYS-002` or `JOB-20260630-924`)
- Bus packet: `agent-bus/logs/JOB-20260630-924.packet.json`

## OBJECTIVE (full)

```
Dashboard live-sync (no React): fix memo/ledger/bus lag + POL-002 server enforcement.

ACCEPTANCE (all required, push main, runnable witness):
1) reconcile-ledger.py: POL-002 auto-stale — for each active task (queued/in_progress/approved/blocked) with last ts >30min, append ONE reconcile event per run: event=wip_stale_detected OR auto-transition to blocked with reason "POL-002: no heartbeat in 30+ min" (idempotent per task per day). Document in memos/policy.md.
2) gate_chat_server.py: add GET /api/live/ledger (tail ceo-ledger.jsonl), GET /api/live/bus (read agent-bus jobs.sqlite via export_bus_live logic inline), POST /api/live/regenerate-memos (run generate-memos.py subprocess, return ok). CORS for dashboard origin.
3) dashboard/config.json: add liveApi pointing at gate server (same host as gateChatApi). dashboard/app.js: if liveApi set, poll /api/live/ledger every 15s and /api/live/bus every 10s; fallback to static files. Shorten GATED_POLL to 12s.
4) droplet cron: add */15 * * * * script sync-dashboard-live.sh that runs reconcile-ledger.py + generate-memos.py + export_bus_live.py, git commit+push if changed (like sync-bus-live.sh).
5) README section: why static+live API beats full React for now; React optional later.

WITNESS: python3 -c "import urllib.request; urllib.request.urlopen(http://127.0.0.1:8788/api/live/ledger)" exits 0 on droplet; reconcile appends stale event on synthetic old task in test or dry-run flag.

Do NOT add React/Node rewrite. Vanilla JS + existing Python bridge only.
```

## CONSTRAINTS

- Keep repo private.
- Do not mutate secrets.
- Work only on your job branch; do not merge to main.
- Report back with summary, files changed, tests run, blockers, and branch name.

